export class ChartData {
    labels: string[];
    datasets: [
        {
            data: number[],
            backgroundColor: string[],
            hoverBackgroundColor: string[]
        }];    
};

